---
---

{'postList':[{% for post in site.posts %}{
        'category' : '{{ post.categories }}',
        'title' : '{{ post.title }}',
        'url' : '{{ site.url }}{{ post.url }}',
        'date' : '{{ post.date | date: "%Y-%m-%d" }}'
    },{% endfor %}]
};